// A function which takes an integer as input and returns true if its even false if its not

function evenFunction(a){
    if(a%2==0){
        return true ;
    }
    else 
    return false ;
}
console.log(evenFunction(7));
console.log(evenFunction(90));
